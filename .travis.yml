before_script:
  - export LANG=en_US.UTF-8
language: objective-c
before_install:
    - brew unlink xctool
    - brew update
    - brew install xctool
    - gem install cocoapods -v '0.32.1'
script: xctool -workspace Project/Injector.xcworkspace -scheme InjectorTests test -sdk iphonesimulator